<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springbootdemo2021.mapper.UserMapper">

<!--    <insert id="insertAddress">
        insert into address (gmt_create, gmt_modified, address_province, address_city,
        address_area, address_name, address_phone, address_details, address_status)
        values (now(), now(),
        #{user_id, jdbcType=BIGINT},
        #{address_province, jdbcType=VARCHAR},
        #{address_city, jdbcType=VARCHAR},
        #{address_area, jdbcType=VARCHAR},
        #{address_name, jdbcType=VARCHAR},
        #{address_phone, jdbcType=VARCHAR},
        #{address_details, jdbcType=VARCHAR},
        #{address_status, jdbcType=VARCHAR})
    </insert>

    <select id = "selectByPrimaryKey" resultType="com.example.springbootdemo2021.model.UserDO">
        select *
        from address
        where id = #{id,jdbcType=BIGINT}
    </select>

    <update id = "updatePhone">
        update t_user
        set gmt_modified = now(),
        phone = #{phone,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>-->

</mapper>